{
  "name": "telegram-scheduler-bot",
  "version": "1.0.0",
  "description": "AWS-based Telegram bot for scheduling calendar events",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && cd lambda && npm install && cd ../cdk && npm install",
    "build": "cd cdk && npm run build",
    "deploy": "cd cdk && npm run deploy",
    "destroy": "cd cdk && npm run destroy",
    "synth": "cd cdk && npm run synth"
  },
  "keywords": ["telegram", "bot", "calendar", "aws", "lambda", "serverless"],
  "author": "",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/telegram-scheduler-bot.git"
  }
}